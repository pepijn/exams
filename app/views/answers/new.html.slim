/.row
  .col-md-6
    hoi
  .col-md-6
    .progress.progress-striped.active
      .progress-bar.progress-bar-success(role="progressbar" style="width: #{(1 - current_user.question_stack.length.to_f / @exam.questions.count) * 100}%")

- if @answer && @answer.correct?
  .alert.alert-success
    strong> ✔
    ' Het antwoord op
    strong>
      = @answer.question
    ' is inderdaad
    strong>
      = @answer.option
- elsif @answer && !@answer.correct?
  .alert.alert-danger
    strong> ✖
    ' Helaas, het antwoord
    strong>
      = @answer.option
    | is niet correct

= form_for Answer.new do |f|
  table.table.table-striped
    thead
      tr
        th
          h5
            = @question
    - @options.each.with_index do |option, i|
      tr class="#{'danger' if option == @option}#{'warning' if option.new_record?}"
        td(style="padding:0")
          label style="display:block; padding:8px"
            input name="answer[option_id]" required=true *{type: 'radio', value: option.id, autofocus: i < 1 || option == @last_answer.option, checked: option == @last_answer.option}
              span< = option

  p
    button(type="submit" class="btn btn-lg btn-primary btn-block" disabled)
      | Antwoord controleren

p.text-muted.pull-right
  | Vraag
  strong<>
    = @question.number
  | uit tentamen
  strong<>
    = @question.exam.date

